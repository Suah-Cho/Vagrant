apiVersion: v1
kind: Pod
metadata:
  name: secret-env-from-all
spec:
  contianers:
  - name: my-container
    image: docker.io/busybox
    args: ['tail', '-f', '/dev/null']
    envFrom:
    - secretRef:
        name: my-password